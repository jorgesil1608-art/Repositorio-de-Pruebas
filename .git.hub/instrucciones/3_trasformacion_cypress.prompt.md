Lee `.github/prompt/2_genera_readme_estructurado.prompt.md` y genera una versión equivalente para Cypress sustituyendo referencias a Playwright por Cypress, `playwright.config.js` por `cypress.config.js`, las carpetas por `cypress/e2e/`, `cypress/support/` y `cypress/fixtures/`, los comandos por `npx cypress run` / `npx cypress open`, los reportes por mochawesome/Allure y añadiendo los patrones Custom Commands y Tasks; conserva la estructura de secciones, el flujo de tres pasos, el checklist y todas las restricciones; guarda el resultado como `.github/prompt/4_genera_readme_cypress_estructurado.prompt.md`.
